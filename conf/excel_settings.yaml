defaults:
    - excel_settings_class_schema
    - _self_


sheetType:
    source: source
    master: master
    compare: compare
    sap: sap
    update: update
statusLabels:
    reference: reference
    new: new
    updated: updated
    current: current
    change: change
columnLabels:
    date: date
    status: status
    search: searchIndex
    partNumber: partNumber
paths:
  log: ./runs
  data: ${hydra:runtime.cwd}/data/
  settingsXML: ${hydra:runtime.cwd}/settingsXML/
  settingsYAML: ${hydra:runtime.cwd}/settingsYAML/